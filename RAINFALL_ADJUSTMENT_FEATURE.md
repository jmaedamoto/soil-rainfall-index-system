# 雨量予想手入力修正機能

本番クライアントのセッションデータに対して、降水量予測を手入力で修正し、対象メッシュの雨量・土壌雨量指数・危険度を再計算する機能です。

## 利用箇所

- クライアント: `client/src/components/RainfallAdjustmentModalSession.tsx`
- クライアント補助: `client/src/features/rainfall-adjustment/`
- APIクライアント: `client/src/services/sessionApi.ts`
- サーバーAPI: `server/src/api/controllers/session_controller.py`
- 計算処理: `server/src/services/rainfall_adjustment_service.py`

## 操作フロー

1. 本番ページでデータを取得し、セッションIDを作成する
2. 雨量調整モーダルを開く
3. 入力モードと補正モードを選ぶ
4. 市町村または二次細分ごとの雨量を入力する
5. `POST /session/<session_id>/recalculate` で再計算する
6. サーバーはフォークセッションを作成し、調整後データを返す
7. クライアントは地図、時系列、雨量データを調整後セッションで表示する

## 入力モード

- 3時間雨量: 各FTの3時間雨量を直接入力する
- 24時間合計雨量: 24時間合計を入力し、選択した補正モードで3時間値へ展開する

## 補正モード

| モード | 内容 |
| --- | --- |
| 3時間比率補正 | 代表3時間雨量に対する入力値の比率を対象メッシュに掛ける |
| 3時間塗りつぶし | 選択時刻の対象メッシュ雨量を入力値で置き換える |
| 24時間均等塗りつぶし | 24時間合計を8区間に均等配分して置き換える |
| 24時間均等比率補正 | 均等配分した3時間値を基準に比率補正する |
| 24時間最大メッシュ塗りつぶし | 対象領域で24時間合計が最大のメッシュの時間分布で配分する |
| 24時間最大メッシュ比率補正 | 最大メッシュの24時間合計を基準に比率補正する |

各モードの説明はUI上のツールチップで確認できます。

## データ更新範囲

再計算後は以下のタイムラインを更新します。

- メッシュ別3時間雨量
- メッシュ別1時間雨量
- メッシュ別SWI
- メッシュ別1時間危険度
- メッシュ別3時間最大危険度
- 市町村・府県の危険度集計

境界メッシュなど複数領域に関係するメッシュは、複数入力のうち保守的な値を採用します。
